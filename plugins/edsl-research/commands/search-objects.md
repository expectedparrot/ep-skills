---
description: Search for EDSL objects on Expected Parrot cloud
argument-hint: <search query or UUID>
allowed-tools: [Read, Glob, Bash, AskUserQuestion, Write]
---

# Search Objects

Search, browse, and pull EDSL objects (Surveys, Results, AgentLists, etc.) from Expected Parrot cloud.

## Arguments

The user provided: $ARGUMENTS

If no arguments were provided, ask: "What are you looking for? Provide a search query, object type, or UUID."

## Instructions

Use the `edsl-research:search-objects` skill to search.

1. Invoke the `edsl-research:search-objects` skill for the full search workflow
2. Search the Expected Parrot cloud for matching objects
3. Present results and allow the user to pull objects locally
